# Arduino-to-C-Emulator-Transpiler
A C# project that converts Arduino/C++ code into an intermediate representation through lexical analysis. The goal is to build a complete compiler pipeline consisting of a lexer, parser, AST, and code generator, enabling future execution, interpretation, or translation of Arduino sketches into the .NET environment.
